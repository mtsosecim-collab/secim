import {env} from 'cloudflare:workers';
const out=(data:unknown,status=200)=>Response.json(data,{status,headers:{'Cache-Control':'no-store'}});
const base64=(bytes:Uint8Array)=>{let s='';for(const b of bytes)s+=String.fromCharCode(b);return btoa(s)};
export async function POST(request:Request){try{const f=await request.formData(),group=Number(f.get('group')),candidates=String(f.get('candidates')||'').trim(),contact=String(f.get('contact')||'').trim(),color=String(f.get('color')||''),file=f.get('image');if(!Number.isInteger(group)||!/^#[0-9a-fA-F]{6}$/.test(color))return out({error:'Komite ve liste rengi zorunludur.'},400);let image:string|null=null;if(file instanceof File&&file.size){if(!file.type.startsWith('image/')||file.size>650000)return out({error:'Görsel JPG, PNG veya WebP ve en fazla 650 KB olmalıdır.'},400);image=`data:${file.type};base64,${base64(new Uint8Array(await file.arrayBuffer()))}`};await env.DB.prepare('INSERT INTO submissions(id,group_id,list_name,candidates,contact_name,image_data,status,created_at,color) VALUES(?,?,?,?,?,?,?,?,?)').bind(crypto.randomUUID(),group,'',candidates,contact,image,'pending',new Date().toISOString(),color).run();return out({saved:true},201)}catch{return out({error:'Başvuru kaydedilemedi.'},503)}}